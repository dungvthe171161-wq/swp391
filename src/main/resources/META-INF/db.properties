# Database runtime configuration is provided by DB_HOST, DB_PORT, DB_NAME,
# DB_USER, and DB_PASSWORD. For non-Docker local overrides, create
# META-INF/db.local.properties; that file is ignored by Git and Docker.
